description: 'Unauthorized error'
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: number
          example: 401
        message:
          type: string
          example: 'Unauthorized'
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Access token not provided'
